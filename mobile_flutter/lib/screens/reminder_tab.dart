// Stub — Phase 3 (see the plan). Kept as a placeholder so the app shell
// compiles/runs while Phase 1 (Report tab) is being validated.
import 'package:flutter/material.dart';

class ReminderTab extends StatelessWidget {
  final int? profileId;
  const ReminderTab({super.key, required this.profileId});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Reminder — coming in Phase 3'));
}
