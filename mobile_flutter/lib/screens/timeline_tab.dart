// Stub — Phase 2 (see the plan). Kept as a placeholder so the app shell
// compiles/runs while Phase 1 (Report tab) is being validated.
import 'package:flutter/material.dart';

class TimelineTab extends StatelessWidget {
  final int? profileId;
  const TimelineTab({super.key, required this.profileId});

  @override
  Widget build(BuildContext context) => const Center(child: Text('Timeline — coming in Phase 2'));
}
