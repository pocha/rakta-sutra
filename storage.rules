rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    // ─── App releases: publicly readable, no auth required ───────────────
    match /releases/{filename} {
      allow read: if true;
      allow write: if false;
    }
  }
}
